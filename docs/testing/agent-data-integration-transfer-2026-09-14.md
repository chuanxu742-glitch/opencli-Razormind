# 首发增量跨分支整合依据

此记录供 #138 的最终协调者使用，不改变其分支、所有权或验收合同。

## 可整合增量

- 共同基线：`6b7c69e0d7f1767d7101336cfeb68e869c772fdf`。
- 已验收功能终点：`94b591a168295a46c1e2af591c6fd06dd6629068`。
- 来源分支：`codex/launch-closeout-20260912-integration`。
- 终点相对基线包含 27 个文件：通用知识库及项目绑定、旧品牌迁移、下游显式绑定授权、通用知识/记录界面、响应体与正文分离限制、回归及验收文档。
- 用户原主工作区保留原有修改；上述增量已分批应用并逐文件比对，不可把整个主工作区当作本任务提交。
- 共同基线以前包含私有快照；禁止为交付直接推送完整来源分支。最终协调者应审查并移植任务增量到其正确发布基线。

## 已有证据

| 范围 | 新鲜证据 | 证据边界 |
| --- | --- | --- |
| 通用知识与下游授权 | 49 项后端测试；15 项前端测试和类型检查；真实无品牌创建、上传、REST/MCP 绑定与撤销、浏览器 | SQLite 迁移已验收，未实测 PostgreSQL |
| 抓取响应限制 | 整合后研究服务/API 29 项；独立审查；真实 92KB 页面从失败变为抓取成功 | 正文仍 48K 字符，解码响应限制 256K 字节 |
| 实际变化消费 | 真实同 URL 内容变化、基线/哈希/diff、HTTP MCP、无 mock 浏览器展示 | 公开时间文本，非竞品语义分析 |
| 身份与运行健康 | 最终 5 个 REST 端点及 HTTP MCP 查询成功；无调用方身份为 401 | 隔离 API 8130，不代表另一分支运行验收 |

详细证据见 [通用知识库](general-knowledge-live-acceptance-2026-09-14.md)、[变化与抓取](research-watch-live-acceptance-2026-09-14.md) 和 [首发链路](launch-live-acceptance-2026-09-14.md)。测试数量存在重叠，不相加。

## 最终协调者必须继续验证

1. 与当前主线和 #135–#137 的交付整合，处理实际冲突，不能只合并本文档。
2. 在最终代码上重跑受影响检查和真实来源/MCP 消费；本分支的绿色结果不能自动证明最终合并结果。
3. 配置可用 chat 默认模型，验证实际采集、有效原文引用的模型分析及下游消费同一成果。当前 `analysis_ready=false`，不得称完整链路通过。
4. 保持项目隔离、旧产品范围、解绑不重新授权及无身份拒绝；不得为了兼容扩大数据范围。
5. 只有满足完整父任务和最终集成合同才关闭 #130。本线程不自行关闭或覆盖 #138。

路由：后端和界面分别使用隔离 worktree，主 Agent 集成与真实验证，独立 Sol High 审查权限、迁移、抓取限制和父目标。复核发现的文档漂移、界面保存保护及抓取响应限制已修正。
