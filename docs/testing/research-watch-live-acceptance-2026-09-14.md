# 真实变化监测补充验收

日期：2026-09-14。独立运行环境仍为 API 8130 / 前端 3130。

## 实际链路

平台从同一个公开文本地址 `https://wttr.in/Shanghai?format=%T` 获取内容，未改写数据库或模拟 HTTP 响应。

- 成功基线运行：`3f891900-f665-56b4-908a-b6206a051816`，正文 `14:22:29+0800`。
- 变化运行：`d304881a-14ee-5f49-bced-d07775d3173c`，正文 `14:27:38+0800`。
- 前哈希：`519c9fd68fe7d02f04a0a1f56d9c6ea151a9324b33ba841292a36775dc46bac1`。
- 后哈希：`e651327301e6ea0229281486ab170b574570d90bba4020f145920cba6744aaae`。
- REST 持久化结果标记 `changed`，基线引用及前后哈希与读取的历史运行一致，结构化 diff 非空。
- 实际 HTTP MCP `get_project_research_run` 返回相同后哈希和变化结果。
- 实际浏览器 `/launch` 显示该运行、基线、变更前后及 diff，逐项匹配持久结果，无页面异常。截图已经人工目视核对。

初始两次相邻抓取返回相同文本，正确显示 `unchanged`；稍后公开服务内容变化后才获得上述结果。没有为了通过验收虚构变化。

本机私有证据：`live-changed-watch-evidence.json`、`browser-changed-watch-evidence.json`、`changed-watch-live.png`，与凭据分开保存在私有验收目录，不提交数据库或令牌。

## 边界与剩余项

这是公开动态文本的真实传输、基线、差异、持久化和消费验证，不能称为真实竞品价格变化或语义分析。当前 `analysis_ready=false`，运行保持 `partial`，未生成模型结论。

本轮还观察到 timeanddate 的 HTTP 403 和 time.is 约 92KB HTML 触发旧 48KB 响应限制；这些失败均保留在运行结果中。

## 抓取限制修复验收

独立 worktree 提交 `5a53fd4c` 经独立审查后整合为 `990dd4e8`。解码后响应体现在独立限制为 256,000 字节，提取正文仍限制为 48,000 字符；超限流式响应明确失败，SSRF、类型和禁止重定向校验不变。

研究服务 23 项测试通过；独立审查另行验证 3 项新测试通过，覆盖大 HTML/短正文、压缩流解码后超限、正文截断。整合后研究服务与 API 共 29 项通过（8.98 秒）。上述数量有重叠，不相加。

重启隔离 API 后，平台重新抓取同一 `https://time.is/UTC` 成功：运行 `4fc1ea3e-1bb7-5d7d-8244-d2a32f820875`，来源状态 `fetched`，正文哈希 `5e1999bfb2c36daa0f7bb6f12b2501cbf6ae7401a0daf203385507c90843355f`。仅剩模型未配置缺口，未将该次采集宣称为分析成功。证据保存在私有 `live-large-page-evidence.json`。

完整模型链路仍需可用的 chat 默认模型。另有 #138 所属独立集成分支及 #135–#137 工作，应由其协调者与本线程已验证增量整合；本报告不宣布父任务 #130 完成，也不覆盖另一分支。
